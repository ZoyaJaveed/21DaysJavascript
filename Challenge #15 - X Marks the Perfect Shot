const finalPosition = (moves) => {
  
  // Code here!
  var x = 0, y = 0;
  
  for(i=0;i<moves.length;i++){
        switch(moves[i]){
      
    case 'north': ++y;
                  break;
    case 'south': --y; 
                  break;
    case 'east' : ++x;
                  break;
    case 'west' : --x;
                  break;
    }
  }
  
  return [x,y];

};
